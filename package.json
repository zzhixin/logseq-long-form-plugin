{
  "name": "logseq-long-form-plugin",
  "version": "0.2.0",
  "private": true,
  "description": "A rebuilt long-form writing experience for Logseq with cleaner reading modes, heading-aware structure helpers, list polishing, and markdown export.",
  "type": "module",
  "main": "dist/index.html",
  "logseq": {
    "id": "logseq-long-form-plugin",
    "title": "Long Form Plugin",
    "icon": "./icon.svg"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prepare-dist.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@logseq/libs": "^0.0.17"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
